{
  "name": "api-profiler",
  "version": "0.0.0",
  "description": "Terminal client for api-performance-profiler: route table, recordings and load runs without VS Code.",
  "license": "MIT",
  "bin": {
    "api-profiler": "dist/bin.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('dist/bin.js', 0o755)\""
  },
  "devDependencies": {
    "@api-profiler/core": "^0.0.0",
    "@api-profiler/node": "^0.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}

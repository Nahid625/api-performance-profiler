{
  "name": "api-performance-profiler",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/core",
    "packages/node",
    "packages/express",
    "packages/nestjs",
    "packages/vscode-extension",
    "examples/*"
  ],
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "build": "npm run build --workspaces --if-present",
    "verify": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "jest": "^30.5.1",
    "ts-jest": "^29.4.12",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.70.0"
  }
}
